# URL of the multiplayer WebSocket server the Electron client connects to.
# Exposed to the renderer via Vite (must be prefixed with VITE_).
VITE_MULTIPLAYER_SERVER_URL=ws://localhost:3001

# Port the standalone multiplayer server listens on.
MULTIPLAYER_SERVER_PORT=3001
